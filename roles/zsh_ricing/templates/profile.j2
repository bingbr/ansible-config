# Managed by fedora-workstation-ansible (zsh_ricing).
if [ -d "$HOME/.config/fedora-workstation/env.d" ]; then
  for workstation_env in "$HOME"/.config/fedora-workstation/env.d/*.sh; do
    [ -e "$workstation_env" ] || continue
    . "$workstation_env"
  done
  unset workstation_env
fi
