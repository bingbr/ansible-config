# Managed by fedora-workstation-ansible (web_dev).
export BUN_INSTALL="$HOME/.bun"
case ":$PATH:" in
  *":$BUN_INSTALL/bin:"*) ;;
  *) export PATH="$BUN_INSTALL/bin:$PATH" ;;
esac

[ ! -r "$HOME/.vite-plus/env" ] || . "$HOME/.vite-plus/env"
